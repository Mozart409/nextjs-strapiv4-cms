pre-commit:
  parallel: true
  commands:
    biome:
      tags: frontend style
      glob: '*.{js,ts,jsx,tsx,json,yaml,yml,md}'
      run: dprint fmt
commit-msg:
  jobs:
    - name: cog verify
      run: cog verify --file {1}
