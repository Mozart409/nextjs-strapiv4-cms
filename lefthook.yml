pre-commit:
  parallel: true
  commands:
    biome:
      tags: frontend style
      files: git diff --name-only main
      glob: '*.{js,ts,jsx,tsx}'
      run: pnpm run --filter frontend fmt {files}
